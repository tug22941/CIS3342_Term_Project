import { each } from "jquery";

class Order {
	//constructor
	constructor(gameId, customerId, price, quantity) {
		this.gameId = gameId;
		this.customerId = customerId;
		this.price = price;
		this.quantity = quantity;	
  }
}

function populateShoppingCart() {
	
}